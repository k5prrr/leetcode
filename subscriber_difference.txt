Ручное сравнение подписчиков git


Array.from(document.getElementById("user-profile-frame").getElementsByClassName("Link--secondary")).map(ob => ob.innerHTML).join("\n")

1 Своих подп
let mk = ``.split("\n").reduce((acc, s) => {acc[s]=true;return acc}, {})

2 на кого подп
``.split("\n").filter(s => !mk[s]).map((s, i) => `${i+1} https://github.com/${s}`).join("\n")

- - -

followers
Array.from(document.getElementById("user-profile-frame").getElementsByClassName("Link--secondary")).map(ob => ob.innerHTML).join("\n")


let followers = ``
let mk = followers.split("\n").reduce((acc, s) => {acc[s]=true;return acc}, {})
Array.from(document.getElementById("user-profile-frame").getElementsByClassName("Link--secondary")).map(ob => ob.innerHTML).filter(s => !mk[s]).map((s, i) => `${i+1} https://github.com/${s}`).join("\n")


пирагова 12
14 30


паспорт
инн

категор физ лицо
фин отдел
приём деклорации


